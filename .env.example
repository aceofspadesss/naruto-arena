# Version to load ('default' uses root views/public/data, anything else uses versions/<VERSION>/)
VERSION=default

# Server
PORT=3000
SESSION_SECRET=naruto-arena-secret
SITE_URL=http://localhost:3000

# SMTP (email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Naruto Arena <noreply@naruto-arena.com>

# CPU opponent
AI_ENABLED=true
